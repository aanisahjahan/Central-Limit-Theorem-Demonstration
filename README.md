# Central-Limit-Theorem-Demonstration
The aim is to demonstrate how the CLT works by showing that regardless of the initial distribution (whether normal, binomial, or Poisson), the means of large samples from these distributions will tend to form a normal distribution as the number of samples increases.
